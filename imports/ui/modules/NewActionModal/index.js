import React from 'react';

import Modal from '../../components/Modal';

const NewActionModal = ({ onClose }) => {
  return (
    <Modal onClose={onClose}>
      This is a modal
    </Modal>
  );
};

export default NewActionModal;
